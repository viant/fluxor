// Package clock provides test-friendly wrappers around the standard library
// time utilities.  It is located under `internal` because it is an
// implementation detail and should not be imported by external code.
package clock
